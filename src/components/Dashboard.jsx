// src/components/Dashboard.js
import React from 'react';

const Dashboard = () => {
    return (
        <div>
            <h1>Welcome to the Dashboard</h1>
            <p>This is where authenticated users can see their content.</p>
        </div>
    );
};

export default Dashboard;
